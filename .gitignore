# Secrets — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Excel source files — not needed by the app
*.xlsx
*.xls

# Dev/sandbox HTML files — only the mockup-v2 template is used by the app
draftmap-mockup.html
draftmap-sandbox.html
draftmap-design-test.html

# Project planning docs — local use only
DraftMap - ChatGPT Prompts.md
DraftMap - Claude Efficiency Guide.md
DraftMap - Project Plan.md
